# 🚀 Развертывание Between The Lines на Replit

## 📋 Пошаговая инструкция

### 1. Создание нового Repl

1. Перейдите на [replit.com](https://replit.com)
2. Нажмите "Create Repl"
3. Выберите "Python" как язык
4. Назовите проект "between-the-lines"
5. Нажмите "Create Repl"

### 2. Загрузка файлов

**Вариант A: Через Git (рекомендуется)**
```bash
# В консоли Replit выполните:
git clone https://github.com/your-username/between-the-lines.git
cd between-the-lines
```

**Вариант B: Ручная загрузка**
1. Скопируйте все файлы из локального проекта
2. Вставьте их в файловую структуру Replit

### 3. Настройка переменных окружения

1. В левой панели найдите "Tools"
2. Выберите "Secrets"
3. Добавьте следующие переменные:

```
OPENAI_API_KEY = your_actual_openai_api_key_here
```

### 4. Установка зависимостей

В консоли Replit выполните:
```bash
pip install -r requirements.txt
```

### 5. Запуск системы

1. Нажмите кнопку "Run" в верхней панели
2. Или выполните в консоли:
```bash
python3 run_replit.py
```

## ⚙️ Настройка автоматического запуска

### Вариант 1: UptimeRobot (рекомендуется)

1. Зарегистрируйтесь на [uptimerobot.com](https://uptimerobot.com)
2. Добавьте новый монитор:
   - Type: HTTP(s)
   - URL: `https://your-repl-name.your-username.repl.co`
   - Monitoring Interval: 5 minutes

### Вариант 2: Cron Job в Replit

Добавьте в `run_replit.py`:
```python
import schedule
import time

def run_scheduled():
    schedule.every().monday.at("09:00").do(main)
    
    while True:
        schedule.run_pending()
        time.sleep(60)
```

## 📁 Структура файлов на Replit

```
between-the-lines/
├── .replit                 # Конфигурация Replit
├── replit.nix             # Зависимости Nix
├── run_replit.py          # Точка входа для Replit
├── main.py                # Основная система
├── config.py              # Конфигурация
├── news_gatherer.py       # Сбор новостей
├── scorer.py              # Оценка важности
├── ai_analyst.py          # AI анализ
├── content_generator.py   # Генерация контента
├── requirements.txt       # Python зависимости
├── .env.example           # Пример переменных окружения
├── README.md             # Документация
└── output/               # Выходные файлы (создается автоматически)
```

## 🔧 Устранение неполадок

### Ошибка: "Module not found"
```bash
pip install -r requirements.txt
```

### Ошибка: "OPENAI_API_KEY not found"
1. Проверьте настройки Secrets в Replit
2. Убедитесь, что переменная называется точно `OPENAI_API_KEY`

### Ошибка: "Permission denied"
```bash
chmod +x run_replit.py
```

### Медленная работа
- Replit может быть медленным при первом запуске
- Подождите 1-2 минуты для инициализации

## 📊 Мониторинг

### Просмотр логов
- Логи отображаются в консоли Replit
- Файлы сохраняются в папке `output/`

### Просмотр результатов
1. Откройте панель "Files"
2. Перейдите в папку `output/`
3. Найдите созданные файлы с дайджестами

## 🔄 Обновление системы

1. Загрузите новые файлы в Replit
2. Перезапустите систему кнопкой "Run"
3. Или выполните в консоли:
```bash
python3 run_replit.py
```

## 💡 Советы

- **Регулярные запуски**: Настройте автоматический запуск раз в неделю
- **Резервное копирование**: Экспортируйте важные файлы из папки `output/`
- **Мониторинг**: Следите за логами для выявления проблем
- **API лимиты**: Следите за использованием OpenAI API

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли Replit
2. Убедитесь в корректности API ключей
3. Проверьте подключение к интернету
4. Перезапустите систему
